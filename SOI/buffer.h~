#ifndef _buffer_h
#define _buffer_h

#include <sys/shm.h>

struct buffer_struct
{
	char letter;
	int size;
};

	
typedef struct buffer_struct Buffer;

int get_buffer_size(Buffer* b);

char pop_buffer(Buffer* b);

void push_buffer(Buffer*, char a);

void init_buffer(buffer* b);

#endif
